package algorithm5.homework4;

import static org.junit.jupiter.api.Assertions.assertTrue;

import java.util.Arrays;

import org.junit.jupiter.api.Test;

public class MainTest {
    // @Test
    // void contextTest1() {
    // long[] digits = Arrays.stream("10 1 10 3
    // 4".split("\\s+")).mapToLong(Long::parseLong).toArray();
    // long[][] requests = new long[4][];
    // requests[0] = Arrays.stream("1
    // 10".split("\\s+")).mapToLong(Long::parseLong).toArray();
    // requests[1] = Arrays.stream("2
    // 9".split("\\s+")).mapToLong(Long::parseLong).toArray();
    // requests[2] = Arrays.stream("3
    // 4".split("\\s+")).mapToLong(Long::parseLong).toArray();
    // requests[3] = Arrays.stream("2
    // 2".split("\\s+")).mapToLong(Long::parseLong).toArray();

    // assertTrue("5 2 2 0".equals(Main.getRequestsAnswer(digits, requests)));
    // }

    // @Test
    // void contextTest2() {
    // long[] digits =
    // Arrays.stream("1".split("\\s+")).mapToLong(Long::parseLong).toArray();
    // long[][] requests = new long[3][];
    // requests[0] = Arrays.stream("1
    // 1".split("\\s+")).mapToLong(Long::parseLong).toArray();
    // requests[1] = Arrays.stream("-1000000000
    // -1000000000".split("\\s+")).mapToLong(Long::parseLong).toArray();
    // requests[2] = Arrays.stream("1000000000
    // 1000000000".split("\\s+")).mapToLong(Long::parseLong).toArray();

    // assertTrue("1 0 0".equals(Main.getRequestsAnswer(digits, requests)));
    // }

}
